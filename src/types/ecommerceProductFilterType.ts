export interface EcommerceProductFilterType {
  enterpriseId?: string;
  name?: string;
  slug?: string;
  description?: string;
  price?: number | string;
  sku?: string;
  categoryId?: string;
  enabled?: boolean;
  visible?: boolean;
  search?: string;
}
